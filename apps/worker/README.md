# IgnisLink Workers

Stage 0 worker scaffold for Python Celery tasks and Node BullMQ webhook fan-out.

Provider calls are intentionally stubbed until the Stage 1 ingestion and verification tests
define filter, deduplication, retry, and provenance behavior.
