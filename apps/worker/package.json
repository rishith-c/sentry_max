{
  "name": "@ignislink/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "BullMQ workers for IgnisLink webhook fan-out and outbox delivery.",
  "main": "./dist/bullmq/index.js",
  "types": "./dist/bullmq/index.d.ts",
  "scripts": {
    "bullmq:dev": "tsx src/bullmq/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo 'no lint yet'",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "dependencies": {
    "bullmq": "^5.21.2",
    "ioredis": "^5.4.1",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.2",
    "vitest": "^2.1.3"
  }
}
