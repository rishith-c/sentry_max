# Base URL for the FastAPI backend Agent 2 is exposing.
# When unset the dispatcher console talks to localhost:8000 — same as the
# default in `pnpm --filter @ignislink/api-py dev` / docker-compose.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
