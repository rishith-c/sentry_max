# Generated Contract Models

Pydantic models generated from `packages/contracts` will live here.

Stage 0 keeps the directory intentionally empty and adds a drift test that checks the
TypeScript contract package exports build artifacts and the `./geometry` subpath needed by
Python code generation.
